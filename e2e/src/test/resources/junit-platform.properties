# Four test classes at a time, against the one docker stack.
#
# Classes run concurrently, the methods of a class do not: a class is the unit of parallelism,
# which keeps the per test state simple and lets a class rely on running on a single thread.
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=same_thread
junit.jupiter.execution.parallel.mode.classes.default=concurrent
junit.jupiter.execution.parallel.config.strategy=fixed
junit.jupiter.execution.parallel.config.fixed.parallelism=4
junit.jupiter.execution.parallel.config.fixed.max-pool-size=4
